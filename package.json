{
  "name": "lottery",
  "version": "2.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx hardhat compile",
    "test": "npx hardhat test",
    "clean": "rm -rf artifacts && rm -rf cache && rm -rf coverage && rm -rf coverage.json && rm -rf .solcover.js && rm -rf yarn.lock",
    "cover": "npx hardhat coverage",
    "deploy:testnet": "hardhat run --network bsctestnet scripts/deploy.js"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.4.1",
    "@openzeppelin/contracts": "^4.6.0",
    "@pancakeswap/pancake-swap-lib": "0.0.4",
    "bignumber.js": "9.0.1",
    "dotenv": "8.2.0",
    "hardhat": "^2.9.6",
    "hardhat-gas-reporter": "1.0.4",
    "solidity-coverage": "^0.7.0"
  },
  "devDependencies": {
    "@nomiclabs/buidler": "^1.4.8",
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.6",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.8"
  }
}
